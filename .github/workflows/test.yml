name: Run Tests

on:
  workflow_dispatch:
  push:

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        node: [15]
        os: [macos-latest, windows-latest, ubuntu-18.04]

    steps:
    - uses: actions/checkout@v2
    - run: curl https://skia-canvas.s3.us-east-1.amazonaws.com/dependencies/icudtl.dat -o icudtl.dat
    - run: ls -R
#     - name: Use Node.js ${{ matrix.node-version }}
#       uses: actions/setup-node@v1
#       with:
#         node-version: ${{ matrix.node-version }}
#     - name: Use Rust
#       uses: actions-rs/toolchain@v1.0.6
#       with:
#         toolchain: stable
#     - run: npm ci
#     - run: npm run build
#     - run: npm test
